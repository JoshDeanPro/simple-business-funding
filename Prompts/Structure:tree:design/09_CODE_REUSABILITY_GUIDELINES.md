# Prompt: Code Reusability and Modular Component Setup

Use this prompt to audit code duplication.

---

## Instructions
1. Consolidate repeating structures into shared UI components (e.g. `<Field>`, `<Note>`).
2. Centralize validation schemas in shared modules.
