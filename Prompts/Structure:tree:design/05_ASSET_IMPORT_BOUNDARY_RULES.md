# Prompt: Asset Import Configurations

Use this prompt to verify file paths for public images.

---

## Instructions
1. Verify that all public images, icons, and fonts reside in the `public/` directory.
2. Access assets using absolute root paths (e.g. `/images/stock/hero.jpg`).
