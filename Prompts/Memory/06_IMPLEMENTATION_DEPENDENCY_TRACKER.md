# Prompt: Dependency and Routing Tracker

Use this prompt to audit package imports and project hook declarations.

---

## Instructions
1. Document any updates to `package.json` dependencies.
2. Audit importing hooks in layout templates (e.g. dynamic router routes, page meta files).
3. Ensure no local dev dependencies are committed as production dependencies.
