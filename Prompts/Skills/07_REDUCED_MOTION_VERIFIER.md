# Skill Prompt: CSS Transition Motion Auditor

Use this prompt to check motion variables.

---

## Instructions
1. Verify that all transitions are declared inside the `@media (prefers-reduced-motion: no-preference)` block.
2. Disable layout shifts or hover scaling for users who prefer reduced motion.
