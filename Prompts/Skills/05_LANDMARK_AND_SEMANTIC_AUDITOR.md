# Skill Prompt: HTML5 Semantic Landmark Auditor

Use this prompt to inspect landmark tagging structure.

---

## Instructions
1. Ensure the layout structure utilizes semantic HTML tags (`<header>`, `<main>`, `<footer>`, `<aside>`, `<nav>`).
2. Verify that header landmarks are not nested.
