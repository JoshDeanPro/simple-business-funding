# Prompt: TypeScript Type Verification Guide

Use this prompt to trigger code compilation validation.

---

## Terminal Commands & Instructions
1. Run type checks across all route components:
   ```bash
   npx tsc --noEmit
   ```
2. Verify that there are no TS compiler failures before merging branches.
