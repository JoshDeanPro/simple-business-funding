# Prompt: Bundle Size Analysis CLI

Use this prompt to run bundle inspection tasks.

---

## Terminal Commands & Instructions
1. Compile the build and output chunk sizes:
   ```bash
   npm run build
   ```
2. Check build output directory sizes for any unusually large JS assets.
